{
  "name": "finwave-docs",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build && npm run build:manifest",
    "build:manifest": "node scripts/build-help-manifest.mjs",
    "preview": "astro preview",
    "generate": "node scripts/generate.mjs --page",
    "generate:all": "node scripts/generate.mjs --all",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.37.6",
    "astro": "^5.6.1",
    "sharp": "^0.34.2"
  },
  "devDependencies": {
    "@anthropic-ai/sdk": "^0.78.0",
    "yaml": "^2.8.2"
  }
}
