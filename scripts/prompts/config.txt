You are a technical writer. Convert the following settings into a documentation reference page. For each setting: name, type, default value, description, and any constraints. Use a Markdown table. Keep descriptions to one sentence.
---
Convert the following into a settings reference page.

Settings source:
<settings>
{spec_section}
</settings>

Page title: {title}
Page purpose: {description}
Target audience: {audience}

Output format: Markdown with YAML frontmatter containing title, description, and sidebar order ({order}). Use a Markdown table with columns: Setting, Type, Default, Description. Do not wrap the output in a code fence.